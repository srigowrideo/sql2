SELECT 
ORDER_NUMBER,
ORDER_AMOUNT AS INDIVIDUAL_ORDER_AMOUNT;
SUM(ORDER_AMOUNT)OVER(PARTITION BY ORDER_NUMBER)AS TPTAL_ORDER_AMOUNT
FROM 
ORDER_DETAILS
ORDER BY 
ORDER-NUMBER ASC;
ORDER_AMOUNT DESC;
